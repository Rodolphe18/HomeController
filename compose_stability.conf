// Classes du domaine (:core:model) traitees comme stables par le compilateur Compose.
// Ce sont des value objects immuables (proprietes val, types stables) ; les declarer ici
// permet aux composables feuilles (EntityRow, DeviceRow...) qui les recoivent en parametre
// d'etre skippables, SANS introduire de dependance androidx.compose.runtime dans la couche
// domaine (Clean Architecture).
com.francotte.homecontroller.core.model.**
