<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pas de neverForLocation : la découverte Bluetooth Classic (startDiscovery)
         ne livre ses résultats (ACTION_FOUND) sur Android 12+ que si ce flag est absent
         ET qu'une permission de localisation est accordée (checkScanPermissionForDataDelivery). -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <!-- Requise à tout niveau d'API : sans neverForLocation, startDiscovery() n'aboutit
         (et ne renvoie de résultats) que si la localisation est accordée, y compris sur Android 12+. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />
</manifest>
